@model Bouffage.Models.VoteComment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>VoteComment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="VoteCommentId" />
            <div class="form-group">
                <label asp-for="UserVotedThisComment" class="control-label"></label>
                <select asp-for="UserVotedThisComment" class="form-control" asp-items="ViewBag.UserVotedThisComment"></select>
                <span asp-validation-for="UserVotedThisComment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CommentGotVoted" class="control-label"></label>
                <input asp-for="CommentGotVoted" class="form-control" />
                <span asp-validation-for="CommentGotVoted" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UpOrDown" class="control-label"></label>
                <input asp-for="UpOrDown" class="form-control" />
                <span asp-validation-for="UpOrDown" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
